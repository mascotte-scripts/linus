set sv_entityLockdown "relaxed" #Allow AI but don't allow clients to spawn entities
setr startingBankAmount 35000 #Starting Bank Balance
setr startingWalletAmount 4000 # Starting Wallet Balance
setr DefaultJob "Unemployed" # Default job assigned after first spawn
setr identifierType "license:" #Identifier to use for storing player data
